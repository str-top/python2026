Вопросы с выбором варианта: 11/78%  
# Вопрос 1. Блок 1. Предварительная подготовка
  
### Что нужно сделать до всех заданий

1. Создай папку проекта.
2. Создай файлы:
   - `index.html`
   - `styles.css`
3. Добавь базовую структуру HTML (каркас документа).
4. Подключи CSS через `link`.
5. Выбери тему страницы. Это будет “страница-визитка” с секциями и карточками.

### Правила (обязательные)

- Стили пишем только в `styles.css`.
- В HTML используем семантические теги там, где это уместно.
- Имена классов пишем латиницей.
- В конце занятия должна получиться одна цельная страница, а не набор кусочков.

### Как правильно пользоваться ИИ (чтобы это было обучение)

- Не отправляй в ИИ “сделай красиво”. Отправляй конкретные условия.
- Не копируй результат вслепую. Сначала прочитай и пойми структуру.
- После ИИ обязательно внести изменения по доп. условиям. Это часть сдачи.

---

## Блок 2. Каркас HTML и семантика

### Содержание и вопросы

- Что такое базовая структура HTML-документа?
- Зачем нужны `head` и `body`?
- Какие семантические теги подходят для шапки, основного контента и подвала?
- Как правильно использовать заголовки `h1`-`h3`?

### Материал (лаконично)

- `<!doctype html>` это “режим HTML”. Без него браузер может странно интерпретировать разметку.
- `head` это “настройки страницы”:
  - кодировка, чтобы текст на русском не ломался
  - `viewport`, чтобы страница нормально выглядела на телефоне
  - `title`, чтобы было понятное имя вкладки
  - подключение `styles.css`
- `body` это все, что видит пользователь.
- Семантические теги это понятные названия блоков вместо безликих контейнеров. Они помогают:
  - быстрее ориентироваться в коде
  - лучше понимать структуру страницы
  - подготовить основу для доступности
- Минимальный набор для страницы-визитки: `header` (шапка), `main` (основное), `footer` (подвал).
- Заголовки:
  - `h1` один на страницу, это главный смысл страницы
  - `h2` для секций
  - `h3` для элементов внутри секции, например для карточек
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Это не вопрос, а подтверждение прочтения инструкции. Ответ "Прочитано" означает, что студент ознакомился с правилами и требованиями к проекту. Важно, чтобы он действительно следовал этим правилам, особенно использовал семантические теги и подключал стили правильно.  

---

#  Вопрос 2. Практическое задание (Блок 2)
  
### Условия

Сделай каркас `index.html` пошагово:

1. Создай базовую структуру документа.
2. В теге `html` установи язык документа.
3. В `head` добавь:
   - кодировку
   - настройку `viewport`
   - заголовок вкладки
   - подключение `styles.css`
4. В `body` создай 3 блока:
   - шапка
   - основная часть
   - подвал
5. В шапке добавь:
   - главный заголовок страницы
   - короткую строку-описание
6. В основной части создай минимум 3 секции.
7. В каждой секции добавь заголовок.

Проверка:

- Главный заголовок один.
- Секций в основной части не меньше трех.

### Псевдокод решения

```text
CREATE HTML_DOCUMENT
  SET DOCUMENT_LANGUAGE
  ADD META_CHARSET
  ADD META_VIEWPORT
  SET PAGE_TITLE
  LINK STYLESHEET_FILE

  BODY
    HEADER
      MAIN_TITLE
      SUBTITLE

    MAIN
      SECTION_A
        SECTION_TITLE
        PARAGRAPH

      SECTION_B
        SECTION_TITLE
        CONTENT_PLACEHOLDER

      SECTION_C
        SECTION_TITLE
        CONTENT_PLACEHOLDER

    FOOTER
      CONTACTS_PLACEHOLDER
END
```

### Если использовал ИИ, обязательные изменения вручную

- Переименуй минимум 2 семантических блока (например, добавь `nav` или обертку-контейнер), сохранив смысл.
- Проверь, что заголовки идут по уровням без пропусков.

---

**Вставь решение в комментарий ниже:**
  
### 🟢 Ответ
CREATE HTML_DOCUMENT  
  SET DOCUMENT_LANGUAGE  
  ADD META_CHARSET  
  ADD META_VIEWPORT  
  SET PAGE_TITLE  
  LINK STYLESHEET_FILE  
  
  BODY  
    HEADER  
      MAIN_TITLE  
      SUBTITLE  
  
    MAIN  
      SECTION_A  
        SECTION_TITLE  
        PARAGRAPH  
  
      SECTION_B  
        SECTION_TITLE  
        CONTENT_PLACEHOLDER  
  
      SECTION_C  
        SECTION_TITLE  
        CONTENT_PLACEHOLDER  
  
    FOOTER  
      CONTACTS_PLACEHOLDER  
END  
  
### ИИ анализ
Ответ представляет собой точное воспроизведение псевдокода из условия, который уже является правильным решением. Он корректно отражает все шаги: объявление документа, настройку `head`, создание семантических блоков `header`, `main` с тремя секциями и `footer`. Для улучшения можно было бы явно указать, что `MAIN_TITLE` — это `h1`, а `SECTION_TITLE` — это `h2`, чтобы продемонстрировать понимание иерархии заголовков.  

---

# Вопрос 3. Где находится видимый пользователю контент страницы?
  
  
Варианты ответов:
1) Внутри `meta`
2) Внутри `body`
3) ❌ Внутри `title`
4) Внутри `link`
5) Внутри `head`
  
### ИИ анализ
Правильный ответ — внутри `body`. Тег `body` содержит всё содержимое веб-страницы, которое отображается в окне браузера: текст, изображения, ссылки и другие элементы. Остальные варианты (`meta`, `title`, `link`, `head`) относятся к служебной информации в разделе `head`, которая не отображается напрямую пользователю.  

---

# Вопрос 4. Для чего обычно нужен `meta viewport`?
  
  
Варианты ответов:
1) Для настройки шрифтов
2) Для включения JavaScript
3) ✅ Для нормального вида на телефоне
4) Для создания списка
5) Для вставки картинки
  
### ИИ анализ
Правильный ответ — для нормального вида на телефоне. Метатег `viewport` управляет макетом и масштабированием страницы на мобильных устройствах, обеспечивая корректное отображение контента без необходимости горизонтальной прокрутки. Без него страница может отображаться в уменьшенном виде, как на десктопе, что ухудшает читаемость.  

---

# Вопрос 5. Какой набор тегов лучше описывает структуру страницы-визитки?
  
  
Варианты ответов:
1) `table`, `tr`, `td`
2) ❌ `script`, `style`, `link`
3) `form`, `input`, `button`
4) `b`, `i`, `u`
5) `header`, `main`, `footer`
  
### ИИ анализ
Правильный ответ — `header`, `main`, `footer`. Эти семантические теги идеально описывают основные логические разделы страницы-визитки: шапку, основное содержание и подвал. Остальные наборы предназначены для других целей: таблиц, скриптов и стилей, форм или текстового форматирования, а не для структурной разметки.  

---

# Вопрос 6. Как правильно использовать заголовок `h1` на странице?
  
  
Варианты ответов:
1) ❌ Использовать только в `footer`
2) Использовать в каждой секции
3) Использовать один раз для всей страницы
4) Использовать вместо текста в `p`
5) Не использовать никогда
  
### ИИ анализ
Правильный ответ — использовать один раз для всей страницы. Заголовок `h1` должен быть главным заголовком страницы, отражающим её основную тему, и используется единожды для соблюдения семантики и доступности. Его размещение в каждой секции или в `footer` нарушает логическую структуру и иерархию заголовков.  

---

# Вопрос 7. Для чего нужны семантические теги?
  
  
Варианты ответов:
1) Чтобы структура была понятнее
2) Чтобы заменить CSS
3) Чтобы ускорить интернет
4) Чтобы скрыть контент
5) ❌ Чтобы отключить отступы
  
### ИИ анализ
Правильный ответ — чтобы структура была понятнее. Семантические теги (например, `header`, `article`, `nav`) придают смысл разным частям HTML-документа, делая код более читаемым для разработчиков и понятным для поисковых систем и вспомогательных технологий. Они не влияют на скорость интернета, отступы или визуальное оформление, которое остается задачей CSS.  

---

# Вопрос 8. Где правильнее подключать CSS-файл?
  
  
Варианты ответов:
1) Внутри `meta charset`
2) В `head` через `link`
3) ❌ Внутри комментария
4) Внутри `title` страницы
5) В `body` после `footer`
  
### ИИ анализ
Правильный ответ — в `head` через `link`. Подключение CSS-файла в разделе `head` с помощью тега `<link>` гарантирует, что стили загрузятся до отображения содержимого страницы, предотвращая мерцание нестилизованного контента. Подключение в `body` или в других местах является некорректным и может привести к проблемам с загрузкой стилей.  

---

# Вопрос 9. Зачем добавлять `lang` в теге `html`?
  
  
Варианты ответов:
1) Чтобы указать язык документа
2) Чтобы поменять цвет фона
3) Чтобы включить flexbox
4) Чтобы увеличить заголовки
5) ❌ Чтобы отключить ссылки
  
### ИИ анализ
Правильный ответ — чтобы указать язык документа. Атрибут `lang` в теге `<html>` сообщает браузерам, поисковым системам и программам чтения с экрана основной язык содержимого страницы. Это важно для корректного отображения текста, автоматического перевода, произношения текста вслух и SEO. Он не влияет на стили или поведение элементов.  

---
Вопросы с текстовыми ответами: 1/22%
