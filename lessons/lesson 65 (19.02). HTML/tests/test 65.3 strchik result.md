Вопросы с выбором варианта: 11/78%  
# Вопрос 1. Блок 4. CSS: селекторы, каскад, специфичность, базовые стили
  
### Содержание и вопросы

- Как подключается CSS?
- Что такое селекторы по тегу и по классу?
- Почему класс предпочтительнее id для стилизации?
- Что такое каскад и почему “побеждает” одно правило?

### Материал (лаконично)

- `styles.css` это отдельный файл со стилями. Он подключается в `head`.
- Селекторы:
  - по тегу. Применяются ко всем элементам этого типа
  - по классу. Применяются только там, где ты добавил класс. Это удобнее для проекта
- Каскад это ситуация, когда несколько правил подходят к одному элементу. Тогда “побеждает” более конкретное правило.
- Специфичность в простом виде:
  - id обычно сильнее класса
  - класс сильнее тега
- Для обучения и для маленьких проектов лучше опираться на классы. Так проще поддерживать верстку.
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Этот вопрос проверяет ознакомление с теоретическим материалом. Поскольку отмечен вариант "Прочитано", можно сделать вывод, что студент изучил предоставленные темы. Важно убедиться, что понимание ключевых концепций (подключение CSS, виды селекторов, каскад, специфичность) будет подтверждено в практических и тестовых заданиях.  

#  Вопрос 2. Практическое задание (Блок 4)
  
### Условия

Сделай базовый стиль страницы пошагово:

1. В `styles.css` добавь стиль для страницы целиком (например, для корневого элемента страницы):
   - задай шрифт
   - задай цвет текста
   - задай фон
2. Добавь стиль для ссылок:
   - задай цвет
   - убери или добавь подчеркивание
3. В HTML добавь классы:
   - общий контейнер для содержимого
   - класс для секций
   - класс для карточек проектов
4. В CSS создай правила для этих классов:
   - контейнер. Ограничь ширину и выровняй по центру
   - секции. Добавь вертикальные отступы
   - карточки. Добавь внутренние отступы и рамку или фон

Ограничения:

- Не используй id для стилизации.
- Не используй inline-style.

### Псевдокод решения

```text
IN CSS_FILE
  STYLE PAGE_ROOT
    SET FONT
    SET TEXT_COLOR
    SET BACKGROUND

  STYLE LINKS
    SET LINK_COLOR
    SET DECORATION

  DEFINE CLASS_CONTAINER
    SET MAX_WIDTH
    SET HORIZONTAL_CENTERING

  DEFINE CLASS_SECTION
    SET VERTICAL_SPACING

  DEFINE CLASS_CARD
    SET PADDING
    SET BORDER_OR_BACKGROUND
END
```

### Если использовал ИИ, обязательные изменения вручную

- Добавь отдельный класс-модификатор для одной карточки и сделай заметное отличие.
- Убери хотя бы одно лишнее правило и объясни, почему оно не нужно.

---

**Вставь решение в комментарий ниже:**
  
### 🔴 Ответ
фывафыв  
  
### ИИ анализ
Ответ "фывафыв" не является осмысленным решением практического задания. Он не содержит ни CSS-кода, ни описания выполненной работы, ни ответов на обязательные пункты про класс-модификатор и удаление лишнего правила. Для успешного выполнения задания необходимо предоставить конкретный код стилей и HTML-разметки, следуя пошаговой инструкции.  

# Вопрос 3. Где должны храниться CSS-стили по этому занятию?
  
  
Варианты ответов:
1) ❌ Только в браузере
2) Только в `title`
3) Только в `styles.css`
4) Только в картинках
5) Только внутри `body`
  
### ИИ анализ
Правильный ответ — "Только в `styles.css`", так как в рамках данного занятия рекомендуется использовать внешнюю таблицу стилей для лучшей организации кода. Хранить стили в браузере, тегах `title` или `body`, а также в картинках — технически некорректно или противоречит лучшим практикам. Выбор неверного варианта указывает на необходимость повторить базовые принципы подключения CSS.  

# Вопрос 4. Что делает селектор по тегу, например `p`?
  
  
Варианты ответов:
1) ❌ Меняет только `header`
2) Меняет только один элемент
3) Меняет только ссылки
4) Меняет только картинки
5) Меняет все `p` на странице
  
### ИИ анализ
Селектор по тегу `p` применяет стили ко всем элементам `<p>` (параграфам) на странице, а не к какому-то одному конкретному элементу или другим типам тегов. Выбор неверного варианта показывает путаницу в понимании действия базовых селекторов. Важно запомнить, что селектор по тегу влияет на все элементы данного типа.  

# Вопрос 5. Почему часто удобнее стилизовать через классы?
  
  
Варианты ответов:
1) ✅ Так легче выбирать нужные элементы
2) Так включается JavaScript
3) Так не нужен `body`
4) Так не нужен `head`
5) Так отключаются отступы
  
### ИИ анализ
Классы удобны, потому что они позволяют гибко и точечно выбирать нужные для стилизации элементы, не затрагивая все элементы одного типа на странице. Это повышает управляемость и переиспользуемость кода. Остальные варианты не имеют прямого отношения к преимуществам классов в CSS.  

# Вопрос 6. Что такое каскад в CSS?
  
  
Варианты ответов:
1) ❌ Способ вставки картинок
2) Способ добавить мета-теги
3) Список тегов в HTML
4) Способ рисовать таблицы
5) Правила могут перекрывать друг друга
  
### ИИ анализ
Каскад в CSS — это механизм, который определяет, какое правило стиля будет применено к элементу, когда к нему подходит несколько правил. Ключевая идея в том, что правила могут перекрывать друг друга в зависимости от специфичности, порядка и важности. Выбранный неверный вариант не имеет отношения к определению каскада.  

# Вопрос 7. Что обычно “сильнее” по специфичности?
  
  
Варианты ответов:
1) ❌ комментарий сильнее правила
2) тег сильнее id
3) id сильнее класса
4) пробел сильнее селектора
5) любой текст сильнее CSS
  
### ИИ анализ
В специфичности CSS селектор по id имеет больший вес, чем селектор по классу, который, в свою очередь, сильнее селектора по тегу. Комментарии, пробелы и произвольный текст не влияют на специфичность. Выбор правильного варианта подтверждает понимание иерархии селекторов.  

# Вопрос 8. Что нельзя использовать по условиям блока?
  
  
Варианты ответов:
1) ❌ Файл `styles.css`
2) Классы в HTML
3) Тег `section`
4) Тег `link`
5) Inline-style в HTML
  
### ИИ анализ
Согласно условиям практического задания, для стилизации нельзя использовать inline-style (прямое указание стилей в атрибуте `style` HTML-элемента) и id. Все остальные варианты (`styles.css`, классы, теги `section` и `link`) являются допустимыми и рекомендуемыми инструментами.  

# Вопрос 9. Зачем делать класс-модификатор для одной карточки?
  
  
Варианты ответов:
1) ❌ Чтобы удалить карточку
2) Чтобы показать вариант оформления
3) Чтобы заменить HTML на CSS
4) Чтобы отключить ссылки
5) Чтобы сломать сетку
  
### ИИ анализ
Создание отдельного класса-модификатора для одной карточки позволяет наглядно продемонстрировать, как можно изменять внешний вид отдельных элементов, используя ту же базовую структуру. Это практический приём для выделения элемента без изменения основной логики стилей. Остальные варианты описывают нецелевое использование модификаторов.  

Вопросы с текстовыми ответами: 0/22%
