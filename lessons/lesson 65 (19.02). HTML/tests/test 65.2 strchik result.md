Вопросы с выбором варианта: 47/78%  
# Вопрос 1. Блок 3. Текстовый контент, ссылки, списки, изображения
  
### Содержание и вопросы

- Как сделать ссылку и какие бывают `href`?
- Чем `ul` отличается от `ol`?
- Зачем нужен `alt` у изображения?
- Как структурировать “карточку” проекта только HTML-ом?

### Материал (лаконично)

- Ссылка это тег `a`. Внутри должен быть текст, по которому понятно, куда ведет ссылка.
- `href` может быть:
  - обычным адресом сайта
  - якорем внутри страницы
  - `mailto:` для письма
- Списки:
  - `ul` когда порядок не важен, например “навыки”
  - `ol` когда это шаги, порядок важен
- Картинка это `img`. `alt` обязателен. Если картинка не загрузится, текст `alt` подскажет, что там должно быть.
- “Карточка” проекта это просто блок. Обычно внутри: заголовок, текст, ссылка.
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Ответ "Прочитано" подтверждает ознакомление с материалом, но не демонстрирует понимание.  Для проверки усвоения лучше было бы ответить на конкретные вопросы из раздела "Содержание и вопросы".  В будущем старайтесь пересказывать ключевые идеи своими словами.

#  Вопрос 2. Практическое задание (Блок 3)
  
### Условия

Добавь контент в `index.html` пошагово:

1. В одной из секций добавь заголовок и под ним список.
2. Заполни список из 5 пунктов.
3. В секции с проектами создай контейнер для карточек.
4. Создай минимум 3 карточки. В каждой карточке сделай:
   - заголовок карточки
   - короткий текст-описание
   - ссылку
5. В шапке добавь изображение-аватар.
6. В подвале сделай блок контактов и добавь минимум 3 ссылки.
7. Проверь, что у всех `img` есть `alt`, а у всех `a` есть `href`.

Проверка:

- Все ссылки кликабельны.
- Все изображения имеют `alt`.

### Псевдокод решения

```text
IN SECTION_B
  CREATE LIST
    ADD LIST_ITEM (REPEAT 5 TIMES)

IN SECTION_C
  CREATE CARDS_CONTAINER
    REPEAT 3 TIMES
      CREATE CARD
        ADD CARD_TITLE
        ADD CARD_TEXT
        ADD CARD_LINK

IN HEADER
  ADD IMAGE_WITH_ALT

IN FOOTER
  CREATE CONTACTS_LIST
    ADD LINK_ITEM (REPEAT 3 TIMES)
END
```

### Если использовал ИИ, обязательные изменения вручную

- Замени один тип списка на другой (например, `ul` на `ol` или наоборот) и объясни почему это подходит.
- Добавь еще одну ссылку с другим типом `href` (например, `mailto:`).

---

**Вставь решение в комментарий ниже:**
  
### 🔴 Ответ
asdfasdf  
  
### ИИ анализ
Ответ "asdfasdf" не является осмысленным решением практического задания.  Это выглядит как случайный ввод текста.  Для выполнения задания необходимо было следовать псевдокоду и создать структуру HTML-элементов: списки, карточки проектов, изображение и ссылки в подвале.  Попробуйте выполнить задание заново, опираясь на предоставленный план.

# Вопрос 3. Какой тег используют обычной ссылки?
  
  
Варианты ответов:
1) `span`
2) `p`
3) `div`
4) `img`
5) `a`
6) ⚡ (0.2/1) Я не знаю ответа
  
### ИИ анализ
Правильный ответ — тег `a`.  Он является основным тегом для создания гиперссылок в HTML.  Атрибут `href` внутри этого тега определяет адрес, на который ведет ссылка.  Выбор варианта "Я не знаю ответа" показывает, что эту фундаментальную тему необходимо повторить.

# Вопрос 4. Какой список лучше подходит для “навыки”, где порядок не важен?
  
  
Варианты ответов:
1) ❌ `ol`
2) `table`
3) `ul`
4) `form`
5) `nav`
  
### ИИ анализ
Для перечня навыков, где порядок не имеет значения, следует использовать неупорядоченный список `ul`.  Тег `ol` создает нумерованный список, который подразумевает последовательность или важность порядка элементов.  Ваш выбор `ol` был неверным для данного контекста.

# Вопрос 5. Зачем нужен атрибут `alt` у изображения?
  
  
Варианты ответов:
1) ✅ Для текстового описания картинки
2) Для создания отступов
3) Для изменения размера картинки
4) Для подключения CSS
5) Для выравнивания текста
  
### ИИ анализ
Атрибут `alt` предоставляет текстовое описание изображения.  Это критически важно для доступности (скринридеры зачитывают этот текст), а также отображается, если изображение не может быть загружено.  Ваш ответ верен и демонстрирует понимание одной из ключевых практик веб-разработки.

# Вопрос 6. Какой `href` используют для ссылки на email?
  
  
Варианты ответов:
1) `ftp:`
2) `mailto:`
3) `css:`
4) ❌ `http:`
5) `file:`
  
### ИИ анализ
Для создания ссылки на email используется протокол `mailto:`.  При клике на такую ссылку у пользователя откроется почтовый клиент с предзаполненным адресом получателя.  Выбор `http:` является ошибкой, так как этот протокол предназначен для перехода на веб-страницы.

# Вопрос 7. Что обычно должно быть внутри “карточки проекта”?
  
  
Варианты ответов:
1) Только `meta` теги
2) Только таблица с данными
3) Только `script` код
4) ✅ Заголовок, текст, ссылка
5) Только картинка без текста
  
### ИИ анализ
Карточка проекта — это информационный блок.  Стандартная структура включает заголовок (`h2`, `h3`), краткое текстовое описание и ссылку для перехода к подробностям или самому проекту.  Ваш ответ точен и отражает типичный паттерн в веб-дизайне.

# Вопрос 8. Что проверяет в конце блока про контент?
  
  
Варианты ответов:
1) ✅ Что все ссылки имеют `href`
2) Что нет `section`
3) Что нет `main`
4) Что нет `footer`
5) Что `head` пустой
  
### ИИ анализ
В конце задания важно проверить, что у всех тегов `<a>` указан атрибут `href`, иначе ссылка будет нерабочей.  Также проверяется наличие атрибута `alt` у изображений.  Остальные варианты (`section`, `main`, `footer`) являются валидными и ожидаемыми элементами страницы, их отсутствие не проверяется.

# Вопрос 9. Что важно для текста ссылки?
  
  
Варианты ответов:
1) Чтобы он был на одной букве
2) Чтобы он был скрыт стилями
3) Чтобы он был только цифрами
4) Чтобы он был пустым
5) ✅ Чтобы он был понятным
  
### ИИ анализ
Текст ссылки (анкор) должен быть понятным и описывать, куда ведет ссылка.  Это важно для удобства пользователей и SEO.  Ссылки со словами "здесь" или "кликните" менее информативны.  Ваш ответ верен и подчеркивает принцип доступности и юзабилити.

Вопросы с текстовыми ответами: 0/22%
