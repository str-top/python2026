Вопросы с выбором варианта: 6/100%  
Вопросы с текстовыми ответами: 0/0%  
# Вопрос 1. Тема 1: Условные конструкции и циклы в Python
  
Какие значения в Python считаются ложными (falsy)?
  
Варианты ответов:
1) Только None
2) Только значение 0
3) Значения, приводимые к False
4) Только пустые строки
5) Только отрицательные числа
  
### ИИ анализ  
Правильный ответ — "Значения, приводимые к False". В Python к ложным (falsy) относятся не одно конкретное значение, а целый набор: `False`, `None`, нулевые числовые значения (0, 0.0), пустые коллекции (например, `[]`, `{}`, `""`).  Остальные варианты слишком узкие, так как описывают лишь часть falsy-значений.  Понимание этой концепции важно для корректной работы условных операторов.  
  
# Вопрос 2. Тема 1: Условные конструкции и циклы в Python
  
Зачем используется `elif`?
  
Варианты ответов:
1) ❌ Для обработки исключений
2) Для создания функции
3) Для остановки цикла
4) Для вывода текста
5) Для добавления нового условия
  
### ИИ анализ  
Ключевое слово `elif` используется для добавления нового, альтернативного условия в цепочку `if-else`.  Оно позволяет проверить несколько взаимоисключающих условий последовательно, не создавая вложенных конструкций.  Ваш выбор "Для обработки исключений" неверен, так как для этого в Python используется конструкция `try...except`.  
  
# Вопрос 3. Тема 1: Условные конструкции и циклы в Python
  
В каком случае удобнее использовать `for`?
  
Варианты ответов:
1) Когда нужно повторять бесконечно
2) Когда нужно ждать событие
3) Когда нет итератора
4) Когда условие меняется случайно
5) ✅ Когда известен набор значений
  
### ИИ анализ  
Цикл `for` идеально подходит для ситуаций, когда необходимо перебрать заранее известную или конечную последовательность значений (список, строку, диапазон `range`).  Он автоматически управляет итерацией, избавляя программиста от необходимости вручную отслеживать условие выхода.  Ваш выбор верен, так как это основное предназначение цикла `for`.  
  
# Вопрос 4. Тема 1: Условные конструкции и циклы в Python
  
В каком случае удобнее использовать `while`?
  
Варианты ответов:
1) Когда нужен словарь
2) Когда есть список значений
3) Когда нужна сортировка
4) Когда есть range
5) ✅ Когда условие важно каждый раз
  
### ИИ анализ  
Цикл `while` используется, когда повторение действий зависит от истинности некоторого логического условия, которое может меняться в процессе выполнения.  Он удобен, когда количество итераций заранее неизвестно (например, ожидание ввода пользователя или обработка данных до достижения цели).  Ваш ответ "Когда условие важно каждый раз" верно отражает эту суть.  
  
# Вопрос 5. Тема 1: Условные конструкции и циклы в Python
  
Что делает `break`?
  
Варианты ответов:
1) Останавливает программу
2) Переходит в else блока
3) Пропускает итерацию
4) ✅ Полностью прерывает цикл
5) Перезапускает цикл
  
### ИИ анализ  
Оператор `break` используется для немедленного и полного выхода из цикла (`for` или `while`), прерывая все последующие итерации.  Управление передается строке кода, следующей сразу за циклом.  Ваш выбор верен, в отличие от `continue`, который лишь пропускает текущую итерацию.  
  
# Вопрос 6. Тема 1: Условные конструкции и циклы в Python
  
Что делает `continue`?
  
Варианты ответов:
1) Переходит к else блока
2) Возвращает значение
3) ✅ Пропускает текущую итерацию
4) Завершает программу
5) Завершает цикл полностью
  
### ИИ анализ  
Оператор `continue` прерывает выполнение тела цикла только для текущей итерации и немедленно переходит к проверке условия (в `while`) или к следующему элементу (в `for`).  Он полезен для пропуска определенных шагов внутри цикла.  Ваш ответ "Пропускает текущую итерацию" абсолютно корректен.  
  
# ❌ Вопрос 7. Тема 1: Условные конструкции и циклы в Python
  
Когда выполняется `else` в конструкции `for ... else`?
  
### 🔴 Ответ  
Тест завершен досрочно